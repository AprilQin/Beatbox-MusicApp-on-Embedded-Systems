Beatbox Application:

This is a simple beatbox application with three rhythms. The application has a user interface written in nodejs, html & jquery.
The backend beatbox music app is written in C and communicate with the front end web user interface through UDP protocol. When the backend application starts,
it creates two threads, one thread for continuously playing the musical beats, another thread for listening to UDP messages that is comming from
the front end user interface.



Steps run this application (on beaglebone):

1. make
2. node server.js
3. go to localhost:3042 on browser

